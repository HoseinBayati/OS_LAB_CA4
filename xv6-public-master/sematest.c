#include "types.h"
#include "stat.h"
#include "user.h"

int main(void) 
{
printf(1, "Note: Unix V6 was released in year %d\n", sem_acquire());
printf(1, "Note: Unix V6 was released in year %d\n", sem_release());
printf(1, "Note: Unix V6 was released in year %d\n", sem_init());

    exit();
} 